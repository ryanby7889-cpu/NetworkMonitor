(()=>{const API='../api/billing_overdue.php',$=id=>document.getElementById(id);let t;const money=v=>'Rp '+Number(v||0).toLocaleString('id-ID'),esc=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]));async function load(){try{const p=new URLSearchParams({bucket:$('bucket').value,q:$('search').value,t:Date.now()}),r=await fetch(API+'?'+p,{cache:'no-store'}),d=await r.json();if(!d.success)throw Error(d.message);const s=d.summary||{};$('cnt').textContent=Number(s.count||0).toLocaleString('id-ID');$('amount').textContent=money(s.amount);$('b1').textContent=Number(s['1_7']||0).toLocaleString('id-ID');$('b2').textContent=Number(s['8_30']||0).toLocaleString('id-ID');$('b3').textContent=Number(s['31_plus']||0).toLocaleString('id-ID');const a=d.items||[];$('rows').innerHTML=a.length?a.map(x=>`<tr><td><b>${esc(x.invoice_no)}</b><small>${esc(x.period)}</small></td><td><b>${esc(x.customer_name)}</b><small>${esc(x.pppoe_username)} · ${esc(x.phone)}</small></td><td>${esc(x.due_date)}</td><td><span class="od-badge">${x.days_overdue} hari</span></td><td><b>${money(x.amount)}</b></td><td class="actions"><a href="payments.php?invoice_id=${encodeURIComponent(x.id)}">Catat Bayar</a><a target="_blank" href="invoice.php?id=${encodeURIComponent(x.id)}">Invoice</a></td></tr>`).join(''):'<tr><td colspan="6" class="empty">Tidak ada invoice terlambat.</td></tr>'}catch(e){$('notice').hidden=false;$('notice').textContent=e.message}}$('bucket').onchange=load;$('refresh').onclick=load;$('search').oninput=()=>{clearTimeout(t);t=setTimeout(load,250)};document.querySelectorAll('.od-kpis button').forEach(b=>b.onclick=()=>{$('bucket').value=b.dataset.b;load()});load();setInterval(()=>document.visibilityState==='visible'&&load(),30000)})()
