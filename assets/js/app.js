// Shared Network Monitor UI behavior.
// Keep page-specific JavaScript in each module/page.
document.addEventListener('DOMContentLoaded', () => {
    document.documentElement.classList.add('app-ready');
});
